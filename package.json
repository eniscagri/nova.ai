{
  "name": "nova-ai-chat",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "android:sync": "npm run build --workspace=client && npx cap sync android && node scripts/normalize-android.mjs",
    "android:open": "npm run android:sync && npx cap open android",
    "android:sync:local": "npm run build:android:local --workspace=client && node scripts/sync-android-local.mjs && node scripts/normalize-android.mjs",
    "android:open:local": "npm run android:sync:local && npx cap open android",
    "ai:talimat": "npm run ai:talimat --workspace=server --"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/app": "^7.1.2",
    "@capacitor/browser": "^7.0.5",
    "@capacitor/core": "^7.4.3",
    "@capacitor/local-notifications": "^7.0.7"
  }
}
